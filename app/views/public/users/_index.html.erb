<% users.each do |user| %>
  <div>
    <%= link_to user do %>
      <% if user.profile_image.present? %>
        <%= image_tag user.profile_image, size: '50x50' %>
      <% else %>
        <%= image_tag 'default_profile.png', size: '50x50' %>
      <% end %>
      <%= user.name %>
    <% end %>
  </div>
  <div class="mb-3"></div> <!-- ユーザーの間に余白を追加 -->
<% end %>